# Codex开发约束

所有交流、注释和交接文档使用简体中文。开始修改前必须先阅读`HANDOFF.md`和`docs/`目录。

## 安全边界

- 不得在未备份`AT+GETVER`、`AT+GETCFG`、`AT+GETDEV`结果时修改任何BU03-Kit配置。
- TFT型号为LVSN-TFT-2.4，商家明确要求VCC只能接3.3V，任何TFT引脚都不得接5V。
- ESP32使用外部5V供电时，先断开电脑USB，避免反向灌电。
- 两个Anchor的UART TX禁止并联。ESP32只从Anchor 0的PA2/UART2_TX接收数据。
- 所有故障、ID失配、定位无效和UWB超时必须保持安全闭锁，不得为了演示绕过闭锁逻辑。
- 当前双基站左右角度误差不对称，属于未解决硬件风险。不得用一个全局角度偏置伪装修复。

## 修改要求

- 保持BU03 37字节真实协议回归测试通过。
- 核心算法尽量保持为不依赖ESP-IDF的纯C组件，便于PC单元测试。
- 修改定位、状态机、协议或输出逻辑后，运行`tests\run_host_tests.bat`。
- 修改ESP-IDF工程后，运行`build_idf.bat`。
- GPIO、供电、标定参数或硬件状态改变后，同步更新`HANDOFF.md`和相关`docs/`文档。
- 不提交`build/`、`tests/build/`、串口日志、临时遥测和本机密钥。

## 当前优先级

1. 完成最终机械布局后重新测量两Anchor天线中心坐标并重新标定距离。
2. 解决左右角度不对称；若抬高、清除遮挡、固定天线姿态后仍不合格，启用第三Anchor。
3. 完成正式电源分路、共地和30分钟稳定性测试。
4. 完成动态进入、离开、ID失配、掉线和断电重启验收。

