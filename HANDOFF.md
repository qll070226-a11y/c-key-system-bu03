# C题数字钥匙系统交接说明

更新日期：2026-07-31

## 1. 项目目标

本仓库实现全国大学生电子设计竞赛C题“基于无线通信的数字钥匙实验系统”的门锁端固件。当前硬件路线为：

- 数字钥匙：1块BU03-Kit Tag，ID为0。
- 智能门锁：2块BU03-Kit Anchor、ESP32-S3-N16R8、2.4英寸SPI TFT、4位拨码、3个LED和蜂鸣器。
- 通信和定位：BU03 UWB；ESP32通过Anchor 0的UART2取得双Anchor测距，通过Anchor 0主USB完整帧取得真实Tag ID。
- 当前算法：两路距离分别执行5点中值与EMA滤波，双圆交点求二维位置并选择门锁正前方解；方位角再执行5点中值与自适应EMA滤波，最后执行ID认证与区域状态机。

## 2. 当前完成度

已完成并实物验证：

- ESP32-S3工程可以构建、烧录和启动。
- Anchor 0 UART2到ESP32 GPIO18的37字节二进制帧接收正常，曾观察约47帧/5秒且坏帧为0。
- 帧中A0/A1两路距离可以解析并进入定位流水线。
- TFT实屏显示正常，控制器按ILI9341驱动，文字、方向和颜色正常。
- TFT已改为320x240中文仪表盘，使用54字的16x16精简点阵字库；保留原英文串口日志，新增中文时必须同步运行字库生成器并检查字符覆盖。
- 4位拨码实时修改门锁认可ID正常：全OFF为00，bit0 ON为01。
- `LOCK:CLOSED`时红灯亮；GPIO输出链路正常。
- PC端核心、协议、状态机和比赛场景测试已通过。
- 固件已设置16MB Flash。
- 固件新增每帧`C_KEY_DIAG_V1`，包含原始、校正、滤波距离及定位结果；原`C_KEY_CSV`保持兼容。
- 方位角新增自适应滤波：静止EMA系数0.15，移动EMA系数0.60，快速跟随阈值3度；TFT、状态机和上位机统一使用滤波后角度，UWB丢失或定位无效时会清空角度滤波状态。
- Python调试上位机已完成：串口、实时曲线、二维轨迹、定点统计、线性拟合、CSV记录和回放均已实现。
- 上位机13项核心测试、模拟数据启动和定点采集页截图生成通过。定点采集分别保存基站、标签天线中心高度，距离参考值和线性标定按三维斜距计算；旧版单高度CSV仍可回放。
- 诊断固件已烧录到ESP32 COM21并通过Flash校验。
- 旧Tag已经损坏并停用；2026-07-31重新配置的新Tag通过COM14供电后，UWB链路恢复。
- 实物诊断链路复测通过：10秒收到100条`C_KEY_DIAG_V1`，解析错误0，掩码持续为0x03。
- 厂家主USB协议已实测：Anchor 0原生USB为VID:PID 0483:5740，3秒收到3030字节，即30个101字节完整帧；实测帧含`Tagid=0`，XOR范围为偏移6到97。
- 固件已新增ESP32-S3 USB CDC Host身份接收。最终配置要求主USB真实ID和UART2距离均在500ms内有效；但当前主USB身份线未接，调试构建临时启用`CONFIG_C_KEY_ALLOW_ID_FALLBACK=y`，无真实ID时使用门锁拨码ID，以恢复距离、定位和TFT。该模式不能验证钥匙身份，验收前必须关闭。
- 2026-07-31复测确认：严格身份模式会在主USB身份线未接时出现“上位机原始A0/A1有值，但校正、坐标和TFT定位数据为空，状态为NO_KEY”。烧录上述调试回退配置后，COM21连续8秒收到81帧、解析错误0，末帧`measurement_ready=1`、`pose_valid=1`，滤波距离为1335.5/1316.3mm，坐标为(+0.058,+1.306)m，角度为+1.68度，状态恢复为WELCOME。

尚未完成或尚未验收：

- 正式12V电池和两路DCDC供电尚未接入测试。
- Anchor 0主USB D-/D+/VBUS尚未实际接到ESP32 GPIO19/GPIO20和DCDC 2，门锁端真实身份链路仍未联调。
- 蜂鸣器已更换为低电平触发模块，GPIO8低电平响、高电平关闭；软件脉冲为500ms，仍需实测响声和上电是否误鸣。
- 绿灯、迎宾灯未逐项留下正式验收记录。
- 最终洞洞板、线束、外壳和天线姿态尚未固定。
- 最终装箱后的距离、角度和正前方零点需要重新标定。
- 双基站角度存在明显左右不对称，当前不能宣称满足角度指标。
- 完整动态进入/离开、ID失配、掉线、重启和30分钟老化测试未完成。

## 3. 关键硬件身份

当前这台电脑曾经使用以下COM口，换电脑或重新插拔后编号可能变化：

| 设备 | 角色/ID | 曾用COM口 |
|---|---|---:|
| 原BU03 Tag | 已损坏、停用 | 曾用COM6 |
| 当前BU03 Tag | 重新配置，UWB测距已恢复 | COM14 |
| BU03 Anchor 0 | ID 0，UART2连接ESP32；PC抓包时主USB为COM19、AT口为COM20 | COM19/COM20 |
| BU03-Kit 3 | Anchor 1，ID 1 | COM18 |
| 剩余备用板 | 可作为Anchor 2，具体板号需重新核对 | 未固定 |
| ESP32-S3 CH343 | 门锁主控 | COM21 |

不要依据COM号判断设备，重新连接后应通过设备管理器和BU03配置查询确认身份。

2026-07-31切换PDOA实验前已完成只读AT备份，尚未向两块板发送任何设置或保存命令：

- COM23：Anchor 1，ID 1，Role 1，固件V1.0.0，备份`backups/bu03/com23_20260731_210329.json`。
- COM24：Anchor 0，ID 0，Role 1，固件V1.0.0，备份`backups/bu03/com24_20260731_210340.json`。
- 两块板的`GETCFG`、`GETDEV`和`PDOAGETCFG`原始响应均已保存；切换实验优先使用Anchor 1与备用板，保留Anchor 0和当前Tag作为TWR回退组合。

## 4. 当前标定参数

当前Anchor天线中心暂按以下坐标建模：

```text
Anchor 0 = (-220, 0) mm
Anchor 1 = (+220, 0) mm
门锁中心 = (0, 0)
门锁正前方 = +Y
圆柱半径 = 300 mm
```

当前距离校正：

```text
A0 corrected_mm = raw_mm * 0.998598 - 179
A1 corrected_mm = raw_mm * 0.972894 - 76
vertical_separation_mm = 250
```

两Anchor天线中心间距为440mm。2026-07-31在Anchor高26cm、Tag高1cm、中心线1.0/1.3/2.0/2.5/3.0m采集1000帧，其中2.0m重复一次。使用六组原始距离均值最小二乘得到上述第二版校正。固件保留校正与滤波后的三维斜距用于遥测，只在二维定位前按250mm高度差换算水平距离。六点离线回放预测径向误差约-1.5到+1.0cm、中心线角度约-3.3到+2.7度；这些点参与了拟合，不能代替独立验收。

第二版固件已构建并烧录到COM21，应用镜像343600字节且Flash Hash校验通过。烧录后标签仍在约2.5m中心线位置时，10秒收到94条诊断帧、解析错误0，末帧定位有效，坐标为(+0.252,+2.470)m、角度+5.46度、径向距离约2.483m。距离误差约-1.7cm；角度满足正负10度但仍存在多径导致的横向偏移，必须继续做未参与拟合距离和正负角度独立验收。

## 5. 已知角度风险

以下角度风险数据来自五点新校正之前，仅作为历史问题记录；烧录新参数后必须重新测试。中心线1.3m处曾稳定在+4.6到+9.6度。

右侧+30度点的稳定均值约+22.4度，部分样本误差超过正负10度。左侧-30度点的稳定均值约-70.2度，全部样本明显超差；左侧测试中A1测距约多出208mm。现象很可能由贴近地面、多径、人体或电池遮挡、天线方向性和安装不对称共同造成。

该误差不是一个固定零偏，因此禁止简单增加全局角度修正。最终处理顺序：

1. 三块UWB天线中心同高，建议离地至少20cm做诊断。
2. 清除天线附近金属、电池、DCDC、电线和人体遮挡。
3. 固定Tag和Anchor姿态，确保左右机械布局对称。
4. 重新采集0、正负30、正负45度数据。
5. 若仍不合格，使用第4块BU03-Kit增加Anchor 2，改为三基站定位。

角度自适应滤波只能降低静止随机抖动，不能消除上述多径和测距系统偏差。验收仍以独立角度点的绝对误差为准，不能只看稳定后的曲线是否平滑。

## 6. 软件结构

```text
main/app_main.c                  ESP32任务、UART、USB身份关联、TFT、GPIO和超时闭锁
components/c_key_core/          纯C距离/身份协议、定位、滤波、状态机、显示和遥测
components/c_key_bu03_usb/      ESP32-S3 USB CDC Host与真实Tag ID缓存
components/c_key_io/            拨码、LED和蜂鸣器GPIO
components/c_key_tft/           ILI9341/ST7789 SPI TFT驱动
tests/                          PC端严格警告单元与比赛场景测试
tools/                          标定、遥测提取和示例数据
host/                           Python定位诊断上位机、测试和串口检查工具
docs/                           硬件、协议、测试和参考资料
```

## 7. 快速开始

本机安装ESP-IDF 5.5.4后：

```powershell
.\tests\run_host_tests.bat
.\build_idf.bat
.\flash_idf.bat COM21
.\flash_monitor_idf.bat COM21
cd host
.\setup.bat
.\run.bat
```

COM21仅为历史值，应替换为当前ESP32端口。监视器中按`Ctrl+]`退出。

## 8. 交接后的第一批工作

1. 阅读`docs/HARDWARE.md`，先完成Anchor 0主USB D-/D+/VBUS接线，核对GPIO19/20极性并确认两路DCDC的5V输出没有并联。
2. 备份三块BU03的版本、配置和设备信息，不修改射频配置。
3. 运行PC测试和ESP-IDF构建，建立新队友电脑上的可复现基线。
4. 烧录新增固件后检查日志中`usb_connected=1 usb_id_ok=1 usb_ok`持续增长，再在`host`目录运行`check_serial.bat COM21`复核。
5. 使用上位机复测拨码ID失配、红绿迎宾灯和UWB掉线闭锁。
6. 实测低电平触发蜂鸣器进入迎宾区响500ms，确认上电、UWB掉线和故障状态不误鸣。
7. 完成正式电源，再用上位机对比贴地与抬高条件。
8. 最终固定机械结构后才进行最终距离与角度标定。

## 9. 资料入口

- [硬件与接线](docs/HARDWARE.md)
- [BU03距离与身份协议](docs/BU03_PROTOCOL.md)
- [标定与验收](docs/CALIBRATION_AND_ACCEPTANCE.md)
- [调试上位机](host/README.md)
- [原始题目和厂家规格书](docs/reference/README.md)
